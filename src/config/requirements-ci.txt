# ===== LIGHTWEIGHT CI DEPENDENCIES (optimized for GitHub Actions) =====
# This file reduces install time and disk pressure by excluding heavy optional packages
# suitable for full CI/testing without GPU. Use this instead of requirements.txt in CI workflows.

-r requirements.txt
# Test-only dependencies
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-timeout==2.1.0
pytest-benchmark==4.0.0
docker==7.1.0

'''
requirements.txt has your Key dependencies 
pytest
pytest-asyncio
httpx
'''